{
  "name": "ledger",
  "version": "0.1.0",
  "private": true,
  "description": "Personal finance dashboard — single-file HTML, React, SVG charts.",
  "type": "module",
  "scripts": {
    "build": "python3 scripts/bundle.py",
    "build:example": "python3 scripts/bundle.py --example",
    "test": "node scripts/smoke.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "@neondatabase/serverless": "^1.1.0"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "xlsx": "^0.18.5"
  }
}
