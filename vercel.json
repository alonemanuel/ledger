{
  "buildCommand": "python3 scripts/bundle.py",
  "outputDirectory": "dist",
  "installCommand": "npm install --no-audit --no-fund --omit=dev",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
